# CEVE543Utils

Julia package of extreme value helpers for CEVE 543.
Entry point is `src/CEVE543Utils.jl`; tests are `test/runtests.jl`, run with `julia --project=. -e 'using Pkg; Pkg.test()'`.
Network tests run only with `CEVE543_NETWORK_TESTS=1`.

@.claude/learned-preferences.md
